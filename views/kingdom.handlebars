<section class="container">
  <h3 class="kingdom-name">{{kingdom.name}}</h3>
  <h4>{{}}</h4>
  <h4>{{}}</h4>
  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each kingdom.species as |species|}}
    {{> species.details}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}